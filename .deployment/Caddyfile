{$SERVER_NAME}

log

# www is not the canonical host. Redirect permanently so there is one URL per
# page, which is what the sitemap and the canonical tags already claim.
@non_canonical host www.urbankizomba.eu
redir @non_canonical https://urbankizomba.eu{uri} permanent

route {
    root * /app/public

    mercure {
        transport_url {$MERCURE_TRANSPORT_URL:bolt:///data/mercure.db}
        publisher_jwt {env.MERCURE_PUBLISHER_JWT_KEY} {env.MERCURE_PUBLISHER_JWT_ALG}
        subscriber_jwt {env.MERCURE_SUBSCRIBER_JWT_KEY} {env.MERCURE_SUBSCRIBER_JWT_ALG}
        anonymous
        subscriptions
        {$MERCURE_EXTRA_DIRECTIVES}
    }

    encode zstd gzip

    php_fastcgi php:9000 {
        env HTTPS on
        header_up X-Real-IP {header.X-Real-IP}
        header_up X-Forwarded-For {header.X-Forwarded-For}
        header_up X-Forwarded-Proto {header.X-Forwarded-Proto}
    }

    file_server
}
